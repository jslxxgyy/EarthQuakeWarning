<Window
    x:Class="EarthquakeWaring.App.Windows.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:EarthquakeWaring.App"
    xmlns:pages="clr-namespace:EarthquakeWaring.App.Pages"
    xmlns:extensions="clr-namespace:EarthquakeWaring.App.Extensions"
    xmlns:services="clr-namespace:EarthquakeWaring.App.Services"
    xmlns:wpfui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="地震预警"
    Width="1150"
    Height="625"
    Closed="MainWindow_OnClosed"
    ManipulationBoundaryFeedback="MainWindow_OnManipulationBoundaryFeedback"
    Style="{StaticResource UiWindow}"
    WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <extensions:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  The title bar contains window navigation elements and some Tray related extras.  -->
        <!--  You can put additional controls in the header, such as a search bar.  -->
        <!--  <wpfui:TitleBar.Header />  -->
        <wpfui:TitleBar Title="地震预警(非官方)" Grid.Row="0" />

        <!-- 配置引导/异常提示条：配置不完整或损坏时显示，引导用户前往设置 -->
        <Border
            Grid.Row="1"
            Padding="12,8"
            Background="#E0B3261E"
            Visibility="{Binding BannerVisible, Converter={StaticResource BoolToVisibilityConverter}, Source={extensions:DI Type={x:Type services:ConfigHealthChecker}}}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Foreground="White"
                    Text="{Binding BannerText, Source={extensions:DI Type={x:Type services:ConfigHealthChecker}}}" />
                <wpfui:Button
                    Grid.Column="1"
                    Margin="12,0,0,0"
                    Appearance="Primary"
                    Click="OnNavigatingSettings"
                    Content="去设置" />
            </Grid>
        </Border>

        <Grid
            x:Name="RootMainGrid"
            Grid.Row="2"
            Margin="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!--  This is the main navigation of the application.  -->
            <wpfui:NavigationStore
                x:Name="RootNavigation"
                Grid.Column="0"
                Margin="6,0,6,0"
                Frame="{Binding ElementName=RootFrame}"
                SelectedPageIndex="0">
                <wpfui:NavigationStore.Items>
                    <wpfui:NavigationItem
                        Click="OnNavigatingEarthQuakesList"
                        Content="预警记录"
                        Icon="Globe24" />
                    <wpfui:NavigationItem
                        Click="OnNavigateExamples"
                        Content="示例地震"
                        Icon="Book24" />
                </wpfui:NavigationStore.Items>
                <wpfui:NavigationStore.Footer>
                    <wpfui:NavigationItem
                        Click="OnNavigatingSettings"
                        Content="设置"
                        Icon="Settings24" />
                </wpfui:NavigationStore.Footer>
            </wpfui:NavigationStore>

            <!--  We display our pages inside this element.  -->
            <Border
                Grid.Column="1"
                Background="{DynamicResource ControlFillColorDefaultBrush}"
                CornerRadius="8,0,0,0">
                <Grid>
                    <Frame x:Name="RootFrame" />
                </Grid>
            </Border>
        </Grid>

    </Grid>
</Window>